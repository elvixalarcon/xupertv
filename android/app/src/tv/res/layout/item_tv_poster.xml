<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="148dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="2dp"
    android:background="@drawable/tv_poster_selector"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:focusable="true"
    android:orientation="vertical"
    android:padding="2dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="210dp"
        android:clipChildren="false">

        <ImageView
            android:id="@+id/tv_poster_img"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/tv_card"
            android:scaleType="centerCrop"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/tv_poster_rating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_margin="8dp"
            android:background="@drawable/tv_rating_badge"
            android:paddingStart="8dp"
            android:paddingTop="4dp"
            android:paddingEnd="8dp"
            android:paddingBottom="4dp"
            android:textColor="#f5c518"
            android:textSize="13sp"
            android:textStyle="bold"
            android:visibility="gone" />

        <View
            android:id="@+id/tv_poster_progress"
            android:layout_width="0dp"
            android:layout_height="4dp"
            android:layout_gravity="bottom"
            android:background="@color/tv_gold"
            android:visibility="gone" />
    </FrameLayout>

    <TextView
        android:id="@+id/tv_poster_progress_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:textColor="@color/tv_gold"
        android:textSize="11sp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/tv_poster_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:ellipsize="end"
        android:gravity="center"
        android:includeFontPadding="false"
        android:lineSpacingExtra="1dp"
        android:maxLines="2"
        android:minLines="2"
        android:paddingBottom="2dp"
        android:textColor="@color/tv_text"
        android:textSize="13sp" />
</LinearLayout>
